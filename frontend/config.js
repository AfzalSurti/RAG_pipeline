// Runtime config for the static frontend (loaded before app.js).
//
// Point API_BASE at your deployed backend (Render) origin — no trailing slash.
//   production : "https://exammind-api.onrender.com"
//   local dev  : "" (empty) if Flask serves this page, or
//                "http://127.0.0.1:8000" if you run the frontend separately.
window.API_BASE = "https://REPLACE-WITH-YOUR-RENDER-URL.onrender.com";
